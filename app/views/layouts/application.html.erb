<!DOCTYPE html>
<html>
  <head>
    <title>CoyoMart</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
      <div class="navbar">

        <h1 class="logo">COYOmart</h1>

        <ul class="navbar-nav">
          <li class="nav-item">
            <%= link_to "Dashboard", root_path, class: "nav-link" %>        
          </li>
          <li class="nav-item">
            <%= link_to "Categorias", categories_path, class: "nav-link" %>        
          </li>
          <li class="nav-item">
            <%= link_to "Produtos", products_path, class: "nav-link" %>
          </li>
        </ul>
      </div>

      <main>
        <!-- Header -->  
        <div class="header-container">
          <div class="header-container">
            <% path = request.fullpath -%>
            <% if path.include? "products" %>
              <h1>Produtos</h1>
            <% elsif path.include? "categories" %>
              <h1>Categorias</h1>
            <% else %>
              <h1>Dashboard</h1>
            <% end %>
          </div>
        </div>

        <%= yield %>

      </main>
  </body>
</html>
